version: "3.9"

services:
  webclient:
    build:
      context: ./webclient/
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./webclient:/app
      - /app/node_modules
